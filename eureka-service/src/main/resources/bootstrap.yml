spring:
  profiles:
    active: ${ENVIRONMENT}
  application:
    name: eureka-service
  cloud:
    config:
      username: root
      password: s3cr3t

management:
  endpoints:
    web:
      exposure:
        include: "*"