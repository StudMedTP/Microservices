spring:
  profiles:
    active: ${ENVIRONMENT}
  application:
    name: notification
  cloud:
    config:
      username: root
      password: s3cr3t
  rabbitmq:
    port: 5672
    username: guest
    password: guest