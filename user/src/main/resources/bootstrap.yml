spring:
  profiles:
    active: ${ENVIRONMENT}
  application:
    name: user
  cloud:
    config:
      username: root
      password: s3cr3t

blockchain:
  rpc: ${SEPOLIA_RPC_URL}
  private: ${SEPOLIA_PRIVATE_KEY}
  contract: ${ATTENDANCE_CONTRACT_ADDRESS}